# Discord bot token — get from https://discord.com/developers/applications
DISCORD_TOKEN=your_bot_token_here

# Port (optional — defaults to 3000, most platforms set this automatically)
PORT=3000

# Optional: comma-separated admin Discord user IDs (also configurable via panel)
ADMIN_IDS=
